# Django settings
DJANGO_SECRET_KEY=django-insecure-dev-key-for-local-development-only
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Database settings
DB_NAME=menu_matching
DB_USER=root
DB_PASSWORD=rootpassword
DB_HOST=db
DB_PORT=3306

# CORS settings
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# FastText model path
FASTTEXT_MODEL_PATH=/app/models/menu.bin

# Mecab 한글 사전 경로 (여러 환경/도커에서 사용 시 ENV로 지정하는 것이 안전)
# Docker 빌드 시 make install 기본값: /usr/local/lib/mecab/dic/mecab-ko-dic
MECAB_DIC_PATH=/usr/local/lib/mecab/dic/mecab-ko-dic
